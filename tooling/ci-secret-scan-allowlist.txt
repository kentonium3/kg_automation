# One relative path per line; use forward slashes.
# Files listed here are *excluded* from the secret-pattern scan.
tooling/scripts/validate_docs.py
# openclaw-auth-verifier (#597) test fixtures and the WP01 prompt that documents
# them. Contain deliberately key-shaped sentinel values used to verify the
# no-key-in-output invariant. Not real Anthropic keys.
tests/security/fixtures/build_fixtures.py
tests/security/test_anthropic_rotate_gate.py
kitty-specs/openclaw-auth-verifier-01KV0Y9E/tasks/WP01-detection-core-and-bash-entry.md
# autonomous-run guard (#964) test fixture. Contains "ghp_" + 36 literal 'A'
# characters — a synthetic probe, annotated as such in place, in the test suite
# for the guard whose job is redacting credentials from denials and ledgers.
# It must stay full length: the test verifies a REAL-length token is redacted,
# so shortening it below the 36-char pattern threshold would silence the
# scanner by weakening the test. Not a real credential.
.agents/autonomous-run-guard/tests/hooks/test_upstream_filing_guard_fixes.py
